<!DOCTYPE html>
<html>
<head>
  <title>SisBec (Alpha)</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
	<%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= javascript_include_tag :defaults %>
  
</head>

<body>
  <% if user_signed_in? %>
  
  Logueado como <strong><%= link_to current_user.username, current_user.student %></strong><%= current_user.admin == true ? ' (admin)' : '' %>.
      <%= link_to "Cerrar Sesión", destroy_user_session_path, :class => 'btn btn-warning'  %>
            <% if !(Student.checkStudent(current_user.id)) && !(request.url.end_with?('/students/new')) && !current_user.admin %>
              <p class="alert alert-danger" role="alert"> No has creado tu perfil de alumno, has <%= link_to 'click aquí', new_student_path, class:'alert-link' %> para ir al formulario de registro </p>
            <% end %>
    <% else %>
        <%= link_to "Nuevo Usuario", new_user_registration_path, :class => 'btn btn-primary'  %> |
        <%= link_to "Logearse", new_user_session_path, :class => 'btn btn-primary'  %>
    <% end %>
  
  <% if notice %>
      <p class="alert alert-notice"><%= notice %></p>
  <% end %>

  <% if alert %>
      <p class="alert alert-error"><%= alert %></p>
  <% end %>

<%= yield %>

</body>
</html>
